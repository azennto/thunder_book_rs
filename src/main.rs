const H: usize = 3;
const W: usize = 4;
const END_TURN: usize = 4;

#[derive(Debug, Clone)]
struct Coord {
    x_: usize,
    y_: usize,
}
impl Coord {
    fn new() -> Self {
        Coord { x_: 0, y_: 0 }
    }
}

fn main() {}
